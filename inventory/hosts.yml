---
all:
  children:
    k3s:
      hosts:
        k3s-node:
          ansible_host: 192.0.2.10   # <-- change to your node IP or hostname
          ansible_user: arch         # <-- ssh user
          ansible_become: true
